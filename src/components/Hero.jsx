import React from 'react';

import '../assets/css/styles.css'; // Import the CSS file

const Hero = () => {
    return (
        <div className="hero">
            <h2>Hero Section</h2>
            <p>This is for hero section</p>
        </div>
    );
};

export default Hero;
